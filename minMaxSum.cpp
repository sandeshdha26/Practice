#include<iostream>
#include<vector>
using namespace std;

void minMax(vector<int> arr)
{
    int total = arr.size();

}

int main()
{
    vector <int> arr;
    cout<<"enter the size: ";
    cin>>arr;
    cout<<"\nMin and Max respectively: ";
    minMax(arr);
    return 0;
}